HELP:
   help       - Show this help message.
   version    - Shows current version.
   list       - List all packages installed.
   install <packageURL>
              - Installs the package from the given URL.
                "http://mervin14.epizy.com/data/testDirForEasy14.zip" is a test package you can install.
   uninstall <packageName>v
              - Uninstalls the package with the given name.
   update <packageName>:
              - Updates the package with the given name.
                "lim update --lim" or "lim update --easy14" only for updating:
                  - Easy14 Programming Language